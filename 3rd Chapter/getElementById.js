// getElementById()

// select element using get element by id
// "getElementById" is a method that returns an HTML element with the specified "id" attribute. It is a part of the Document Object Model (DOM) and is used to access and manipulate HTML elements.

// Syntax:
// document.getElementById(id);
// Where "id" is the value of the "id" attribute of the HTML element you want to access.

const mainHeading = document.getElementById("main-heading");
console.log(mainHeading);
console.log(typeof mainHeading);
